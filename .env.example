# Get your App ID from https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=

# LlamaParse API key for PDF extraction (https://cloud.llamaindex.ai)
LLAMA_CLOUD_API_KEY=

# NEAR AI API key for medical analysis (https://cloud-api.near.ai)
NEAR_AI_API_KEY=

# External specialist verification API (manages specialist data)
# SPECIALIST_VERIFICATION_API_URL=https://your-verification-api.example.com

# Cloudinary for specialist file uploads (photos, documents)
# Use CLOUDINARY_URL or the three separate variables:
# CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# Optional: NEAR network (mainnet | testnet). Defaults to mainnet.
# NEXT_PUBLIC_NEAR_NETWORK=testnet

# Optional: NEAR Intents solver relay API key (Partners Portal: partners.near-intents.org).
# May fix "base58" errors on POA withdrawals; the official app likely uses a partner key.
# NEXT_PUBLIC_NEAR_INTENTS_SOLVER_RELAY_API_KEY=

# Optional: Auto-fund new NEAR implicit accounts (relayer). Pick one:
# (1) Our relayer: set NEAR_FAUCET_ACCOUNT_ID + NEAR_FAUCET_PRIVATE_KEY (testnet or mainnet).
# (2) Testnet only: set NEAR_TESTNET_FAUCET_API_URL to a service that implements POST /near/request
#     with body { network: "testnet", address: "<accountId>", amount: "0.002" } (e.g. faucet-terminal/Near_Faucet).
# NEAR_FAUCET_ACCOUNT_ID=
# NEAR_FAUCET_PRIVATE_KEY=
# NEAR_TESTNET_FAUCET_API_URL=

# Relayer for USDT withdraw: pays gas so the user does not need NEAR. Default relayer account: veridoc.near
# NEXT_PUBLIC_NEAR_RELAYER_ACCOUNT_ID=veridoc.near
# NEAR_RELAYER_ACCOUNT_ID=veridoc.near
# NEAR_RELAYER_PRIVATE_KEY=ed25519:...
